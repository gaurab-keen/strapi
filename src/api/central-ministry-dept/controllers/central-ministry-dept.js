'use strict';

/**
 * central-ministry-dept controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::central-ministry-dept.central-ministry-dept');
