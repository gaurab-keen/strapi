'use strict';

/**
 * central-ministry-dept router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::central-ministry-dept.central-ministry-dept');
