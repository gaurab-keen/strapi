'use strict';

/**
 * central-ministry-dept service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::central-ministry-dept.central-ministry-dept');
