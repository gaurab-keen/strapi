'use strict';

/**
 * location-state-dist router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::location-state-dist.location-state-dist');
